package routes

import (
	//"net/http"

	//"github.com/flavioesteves/wizer-dynamics-go/internal/models"
	"github.com/gin-gonic/gin"
)

func RegisterUserRoutes(rg *gin.RouterGroup) {
	//rg.GET("/get_all_users", getAllUSers)
	//rg.GET("/logged_user", getUserLogged)
	//rg.GET("/:id", getUserByID)

	//rg.POST("/test", addUser)
}
